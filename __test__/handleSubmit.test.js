import { handleSubmit } from "../src/client/js/handleSubmit"

describe('handleSubmit function test', () => {
    test('Check to see if function returns something', () => {           
      expect(handleSubmit).toBeDefined();
    })
  })
  