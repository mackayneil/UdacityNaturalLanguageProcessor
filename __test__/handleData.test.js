import { handleData } from "../src/client/js/handleData"

describe('handleData function test', () => {
    test('Check to see if function returns something', () => {
      expect(handleData).toBeDefined();
    })
  })